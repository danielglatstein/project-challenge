<header>
	<div class="container">
    <%= paginate @dogs %>
	</div>
</header>

<main>
	<div class="container">
		<div class="gallery">
      <%= render partial: 'thumbnail', collection: @dogs, as: :dog %>
		</div>
	</div>
</main>
